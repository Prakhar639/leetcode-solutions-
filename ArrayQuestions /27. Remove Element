class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        for(int i:nums){
            if(i==val){
               // nums.erase(remove(nums.begin(),nums.end(),i),nums.end());//
               erase(nums,i);
            }
        }
        return nums.size();
    }
};
